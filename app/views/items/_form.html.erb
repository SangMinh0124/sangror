<%= form_with(model: item) do |form| %>
  <% if item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(item.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
        <% item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :todoid %>
    <%= form.number_field :todoid %>
  </div>

  <div class="field">
    <%= form.label :cre_item %>
    <%= form.check_box :cre_item %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
